;(function(){
	//"use strict"; // jshint ;_;
	
	$.group.committeemodule = {
			url : "group.committeemodule.html",
			modulehash : '#module=group.committeemodule|{}',
			init:init
	};
	
	function init(param){
		var contentId = param.contentId;
		var $content = $('#'+contentId);
		if($content.length > 0){
			
		}
	}
})();
