;(function(){
	"use strict"; // jshint ;_;
	
	$.clazz.studentpunishmodule = {
			url : "clazz.studentpunishmodule.html",
			modulehash : '#module=clazz.studentpunishmodule|{}',
			init:init
	};
	
	function init(param){
		if(param){
			if(param.data){
			}
		}
	}
})();
