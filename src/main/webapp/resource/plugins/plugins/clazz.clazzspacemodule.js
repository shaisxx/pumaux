;(function(){
	"use strict"; // jshint ;_;
	
	$.clazz.clazzspacemodule = {
			url : "clazz.clazzspacemodule.html",
			modulehash : '#module=clazz.clazzspacemodule|{}',
			init:init
	};
	
	function init(param){
		if(param){
			if(param.data){
			}
		}
	}
})();
