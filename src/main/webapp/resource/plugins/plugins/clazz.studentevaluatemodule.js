;(function(){
	//"use strict"; // jshint ;_;
	
	$.clazz.studentevaluatemodule = {
			url : "clazz.studentevaluatemodule.html",
			modulehash : '#module=clazz.studentevaluatemodule|{}',
			init:init
	};
	
	function init(param){
		var contentId = param.contentId;
		var $content = $('#'+contentId);
		if($content.length > 0){
		}
	}
	
	
	
})();
