;(function(){
	"use strict"; // jshint ;_;
	
	$.clazz.workstudymodule = {
			url : "clazz.workstudymodule.html",
			modulehash : '#module=clazz.workstudymodule|{}',
			init:init
	};
	
	function init(param){
		if(param){
			if(param.data){
			}
		}
	}
})();
