;(function(){
	"use strict"; // jshint ;_;
	
	$.group.groupinfoviewmodule = {
			url : "group.groupinfoviewmodule.html",
			modulehash : '#module=group.groupinfoviewmodule|{}',
			init:init
	};
	
	function init(param){
		//$.changeSelectedTabTitle("魔术队");
		var contentId = param.contentId;
		var $content = $('#'+contentId);
		if($content.length > 0){
			
		}
	}
	
})();
