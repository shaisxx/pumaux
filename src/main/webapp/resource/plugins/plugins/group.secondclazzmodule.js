;(function(){
	//"use strict"; // jshint ;_;
	
	$.group.secondclazzmodule = {
			url : "group.secondclazzmodule.html",
			modulehash : '#module=group.secondclazzmodule|{}',
			init:init
	};
	
	function init(param){
		var contentId = param.contentId;
		var $content = $('#'+contentId);
		if($content.length > 0){
			
		}
	}
})();
